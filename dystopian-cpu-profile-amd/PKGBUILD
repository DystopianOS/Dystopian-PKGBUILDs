# Maintainer: Dystopian <dcxdevelopment at protonmail dot com>
# Co-Maintainer: DCx7C5 <dcxdevelopment at protonmail dot com>

pkgname=dystopian-cpu-profile-amd
pkgdesc="Dystopian AMD CPU profile for Ryzen and newer (metapackage)"
pkgver=0.1.0
pkgrel=1
arch=('x86_64')
url="https://github.com/Dystopian-Project/dystopian-repo"
license=('GPL-3.0-or-later')
conflicts=('dystopian-cpu-profile-intel')
depends=(
    amd-ucode             # Microcode updates for AMD CPUs
    thermald              # Dynamic CPU cooling based on temperature
)
optdepends=(
    "amd-gpu-tools: Useful tools for integrated AMD GPUs (if available)"
    "amd-graphics-compiler: Improved GPU support for AMD CPUs with integrated graphics (if available)"
    "libva-amdgpu-driver: Video acceleration for AMD GPUs (if available)"
)