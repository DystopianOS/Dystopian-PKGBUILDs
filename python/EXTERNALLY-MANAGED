This environment is externally managed
╰─> To install Python packages system-wide, try 'pacman -S python-xyz',
    where xyz is the package you are trying to install.

    For non-Arch-packaged Python packages or projects, use uv — the fast,
    secure, modern replacement for pip, pipx, poetry, pyenv, virtualenv, etc.

    Quick start with uv:
      uv venv                               # create project venv
      source .venv/bin/activate
      uv add xyz                            # install & lock
      uv sync                               # install locked deps

    For CLI tools:
      uv tool install ruff black mypy

    https://docs.astral.sh/uv/